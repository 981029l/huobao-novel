# 流式重构任务说明

**日期**: 2026-02-05
**执行者**: 左岚 (Executor)
**状态**: 进行中

## 任务目标
将核心生成功能重构为流式输出，解决接口超时问题并提供实时反馈。

## 变更内容
1.  **API 层 (`src/api/generator.js`)**:
    - `generateArchitecture`: 增加 `onStream` 回调。
    - `generateChapterBlueprint`: 增加 `onStream` 回调。
    - `generateChapterDraft`: 增加 `onStream` 回调。
    - `finalizeChapter`: 增加 `onStream` 回调。
    - `enrichChapter`: 增加 `onStream` 回调。

2.  **UI 层**:
    - `src/views/ProjectView.vue`: 适配架构和大纲生成的流式调用。
    - `src/components/ChapterWriterPanel.vue`: 适配章节写作和扩写的流式调用。

## 验证方法
- [ ] 假如是架构生成，观察是否逐个字段流式显示。
- [ ] 假如是写作，观察编辑器内容是否打字机式上屏。
