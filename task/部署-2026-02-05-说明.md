# 部署任务说明

**日期**: 2026-02-05
**执行者**: 左岚 (Executor)
**状态**: 进行中

## 任务目标
将应用部署到 `54.80.240.105` 服务器。

## 部署方案
采用 Docker 容器化部署。
1.  **多阶段构建**: 使用 Node 环境构建产物，使用 Nginx 环境提供服务。
2.  **端口映射**: 容器 80 -> 主机 80 (或用户指定端口)。

## 步骤
1.  [ ] **配置调整**: 修改 `vite.config.js` 的 `base` 路径。
2.  [ ] **容器配置**: 创建 `Dockerfile` 和 `nginx.conf`。
3.  [ ] **环境检查**: 检查服务器 Docker 环境。
4.  [ ] **代码传输**: 将源代码 (不含 node_modules) 传输至服务器。
5.  [ ] **构建运行**: 在服务器上构建镜像并启动容器。
