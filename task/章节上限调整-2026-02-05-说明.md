# 章节上限调整任务说明

**日期**: 2026-02-05
**执行者**: 左岚 (Executor)
**状态**: 进行中

## 任务目标
将项目创建时的最大章节数限制从 500 提升至 10000。

## 变更内容
**修改 `src/components/CreateProjectDialog.vue`**:
- 将 `n-input-number` 的 `max` 属性从 `500` 改为 `10000`。
- 将 `n-input` (用户指导) 的 `maxlength` 保持不变或按需调整 (当前未要求)。
- 确认 form rules 中的校验逻辑是否需要调整 (组件属性调整通常不仅限于 template，script 中的 rule 可能也有限制，需检查)。

## 验证方法
1.  代码审查：确认 `max` 属性已更新。
2.  (用户验证) 打开创建项目弹窗，尝试输入 > 500 的数值。
