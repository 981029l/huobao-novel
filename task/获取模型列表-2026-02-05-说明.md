# 模型列表获取任务说明

**日期**: 2026-02-05
**执行者**: 左岚 (Executor)
**状态**: 进行中

## 任务目标
实现从配置的 API Endpoint 动态获取模型列表的功能。

## 变更内容
1.  **API 层 (`src/api/llm.js`)**:
    - 新增 `fetchModels(config)` 函数，调用 GET `/models`。

2.  **状态管理 (`src/stores/settings.js`)**:
    - 新增 `fetchedModels` 状态。
    - 新增 `fetchAvailableModels` Action。

3.  **UI 层 (`src/components/SettingsDialog.vue`)**:
    - 增加刷新按钮。
    - 优先显示 API 返回的模型列表。

## 验证方法
- [ ] 输入 Key 后点击测试连接，检查下拉框是否更新。
- [ ] 点击刷新按钮，检查是否重新获取模型。
